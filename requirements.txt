# Core (install torch first, matched to your CUDA version)
torch==2.6.0

# PyTorch Geometric + extensions
# Install from the matching wheel index, e.g.:
#   pip install torch_geometric
#   pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv \
#       -f https://data.pyg.org/whl/torch-2.6.0+cu124.html
torch_geometric==2.6.1
pyg_lib
torch_scatter
torch_sparse
torch_cluster
torch_spline_conv

# Data / ML
pandas
numpy
scikit-learn
imbalanced-learn

# Hyperparameter search
optuna

# Utilities
tqdm
